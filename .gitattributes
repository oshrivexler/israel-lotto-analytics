# נרמול סופי-שורה: שמירת LF במאגר
* text=auto eol=lf

# קובץ הלוטו בקידוד windows-1255 — לשמור בייט-בבייט (ללא נרמול)
data/lotto.csv -text

# קובצי קיצור-דרך של Windows — לשמור עם סופי-שורה CRLF
*.url text eol=crlf
